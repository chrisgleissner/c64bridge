name: Release NPM Package

on:
  push:
    tags:
      - '*.*.*'
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g., 0.9.3) for re-validation of an already-published release.'
        required: true
        type: string

jobs:
  release:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/${{ github.repository_owner }}/mcp-builder:latest
    defaults:
      run:
        shell: bash
    permissions:
      contents: write
      id-token: write
      packages: read
    outputs:
      pkg_version: ${{ steps.state.outputs.version }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js for release publishing
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'
          package-manager-cache: false

      - name: Determine release state
        id: state
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ inputs.version }}"
          else
            TAG="${GITHUB_REF_NAME}"
          fi
          VERSION="${TAG#v}"

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

          PUBLISHED=$(npm view "c64bridge@${VERSION}" version 2>/dev/null | tr -d '"' || true)
          if [ -z "$PUBLISHED" ]; then
            PUBLISHED=""
          fi

          GITHUB_RELEASE_TAG=""
          if gh release view "$TAG" >/dev/null 2>&1; then
            GITHUB_RELEASE_TAG="$TAG"
          fi

          echo "Detected npm-published='${PUBLISHED}' github-release='${GITHUB_RELEASE_TAG:-<none>}'"

          if [ "$PUBLISHED" = "$VERSION" ] && [ -n "$GITHUB_RELEASE_TAG" ]; then
            echo "publish=false" >> "$GITHUB_OUTPUT"
            echo "skip_prepare=true" >> "$GITHUB_OUTPUT"
            echo "skip_release_notes=true" >> "$GITHUB_OUTPUT"
            echo "Re-validation mode for ${VERSION}: already published to npm and GitHub; skipping publication."
          else
            echo "publish=true" >> "$GITHUB_OUTPUT"
            echo "skip_prepare=false" >> "$GITHUB_OUTPUT"
            echo "skip_release_notes=false" >> "$GITHUB_OUTPUT"
            echo "Fresh release of ${VERSION}: publication required."
          fi

      - name: Determine previous tag
        id: previous
        env:
          CURRENT_VERSION: ${{ steps.state.outputs.version }}
          CURRENT_TAG: ${{ steps.state.outputs.tag }}
        run: |
          node <<'NODE'
          import { execSync } from 'node:child_process';
          import { writeFileSync } from 'node:fs';

          const currentVersion = process.env.CURRENT_VERSION;
          const currentTag = process.env.CURRENT_TAG;

          if (!currentVersion || !currentTag) {
            console.error('Missing current version/tag context');
            process.exit(1);
          }

          const toSemver = (value) => {
            const cleaned = value.startsWith('v') ? value.slice(1) : value;
            const parts = cleaned.split('.').map((n) => Number.parseInt(n, 10));
            if (parts.length !== 3 || parts.some(Number.isNaN)) return null;
            return { cleaned, parts };
          };

          const dedupe = new Map();
          const rawTags = execSync('git tag --sort=version:refname', { encoding: 'utf8' })
            .split('\n')
            .map((tag) => tag.trim())
            .filter(Boolean);

          for (const raw of rawTags) {
            const info = toSemver(raw);
            if (!info) continue;
            if (!dedupe.has(info.cleaned)) dedupe.set(info.cleaned, new Set());
            dedupe.get(info.cleaned).add(raw);
          }

          if (!dedupe.has(currentVersion)) {
            console.error(`Current version ${currentVersion} not found among tags`);
            process.exit(1);
          }

          const compare = (a, b) => {
            for (let i = 0; i < 3; i += 1) {
              if (a.parts[i] !== b.parts[i]) return a.parts[i] - b.parts[i];
            }
            return 0;
          };

          const ordered = Array.from(dedupe.keys())
            .map((cleaned) => ({
              cleaned,
              parts: cleaned.split('.').map((n) => Number.parseInt(n, 10)),
            }))
            .filter((info) => info.parts.length === 3 && !info.parts.some(Number.isNaN))
            .sort(compare);

          const currentIndex = ordered.findIndex((info) => info.cleaned === currentVersion);
          const previousInfo = currentIndex > 0 ? ordered[currentIndex - 1] : null;

          const outputLines = [];

          if (previousInfo) {
            const candidates = Array.from(dedupe.get(previousInfo.cleaned));
            const preferred = currentTag.startsWith('v')
              ? candidates.find((tag) => tag.startsWith('v')) || candidates[0]
              : candidates.find((tag) => !tag.startsWith('v')) || candidates[0];

            outputLines.push(`version=${previousInfo.cleaned}`);
            outputLines.push(`tag=${preferred}`);
          } else {
            outputLines.push('version=');
            outputLines.push('tag=');
          }

          writeFileSync(process.env.GITHUB_OUTPUT, `${outputLines.join('\n')}\n`, { flag: 'a' });
          NODE

      - name: Install dependencies
        run: npm ci

      - name: Run release-prepare
        if: ${{ steps.state.outputs.skip_prepare != 'true' }}
        run: |
          npm run release:prepare -- "${{ steps.state.outputs.version }}"

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit and push release preparation changes
        if: ${{ steps.state.outputs.skip_prepare != 'true' }}
        run: |
          git add package.json package-lock.json mcp.json server.json CHANGELOG.md
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: prepare release ${{ steps.state.outputs.version }}"
            git push origin HEAD:main
          fi

      - name: Build project
        run: npm run build

      - name: Verify local package
        run: npm run check:package

      - name: Install mcp-publisher
        run: |
          curl -L "https://github.com/modelcontextprotocol/registry/releases/latest/download/mcp-publisher_$(uname -s | tr '[:upper:]' '[:lower:]')_$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/').tar.gz" | tar xz mcp-publisher

      - name: Extract changelog entry
        id: changelog
        if: ${{ steps.state.outputs.skip_release_notes != 'true' }}
        run: |
          ENTRY=$(node scripts/extract-changelog-entry.mjs "${{ steps.state.outputs.version }}")
          echo "entry<<EOF" >> "$GITHUB_OUTPUT"
          echo "$ENTRY" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

      - name: Format release notes
        id: notes
        if: ${{ steps.state.outputs.skip_release_notes != 'true' }}
        env:
          CHANGELOG_ENTRY: ${{ steps.changelog.outputs.entry }}
          CURRENT_TAG: ${{ steps.state.outputs.tag }}
          PREVIOUS_TAG: ${{ steps.previous.outputs.tag }}
        run: |
          node <<'NODE'
          import { writeFileSync } from 'node:fs';

          const changelogEntry = process.env.CHANGELOG_ENTRY || '';
          const previousTag = process.env.PREVIOUS_TAG;
          const currentTag = process.env.CURRENT_TAG;

          const lines = changelogEntry.split('\n');
          const bodyLines = [];

          if (lines.length > 0 && /^##\s+/.test(lines[0])) {
            lines.shift();
          }

          while (lines.length > 0 && lines[0].trim() === '') {
            lines.shift();
          }

          bodyLines.push('## What\'s Changed');
          bodyLines.push('');
          bodyLines.push(lines.join('\n').trim() || '- No notable changes in this release.');

          if (previousTag) {
            bodyLines.push('');
            bodyLines.push(`**Full Changelog**: https://github.com/${process.env.GITHUB_REPOSITORY}/compare/${previousTag}...${currentTag}`);
          }

          const output = bodyLines.join('\n').replace(/\n{3,}/g, '\n\n');
          writeFileSync(process.env.GITHUB_OUTPUT, `body<<EOF\n${output}\nEOF\n`, { flag: 'a' });
          NODE

      - name: Update GitHub release notes
        if: ${{ steps.state.outputs.skip_release_notes != 'true' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_BODY: ${{ steps.notes.outputs.body }}
          RELEASE_TAG: ${{ steps.state.outputs.tag }}
        run: |
          node <<'NODE'
          import process from 'node:process';

          const token = process.env.GH_TOKEN;
          const body = process.env.RELEASE_BODY ?? '';
          const tag = process.env.RELEASE_TAG;
          const repository = process.env.GITHUB_REPOSITORY;

          if (!token) {
            console.error('GH_TOKEN not available; cannot update release notes.');
            process.exit(1);
          }

          if (!repository) {
            console.error('GITHUB_REPOSITORY not set.');
            process.exit(1);
          }

          if (!tag) {
            console.error('Release tag missing.');
            process.exit(1);
          }

          const headers = {
            Authorization: `Bearer ${token}`,
            Accept: 'application/vnd.github+json',
            'X-GitHub-Api-Version': '2022-11-28',
          };

          const apiBase = `https://api.github.com/repos/${repository}/releases`;

          async function fetchJson(url, options) {
            const response = await fetch(url, options);
            if (response.status === 204) {
              return null;
            }
            const text = await response.text();
            let json;
            try {
              json = text ? JSON.parse(text) : null;
            } catch (error) {
              console.error('Failed to parse GitHub API response:', text);
              throw error;
            }
            return { response, json };
          }

          async function main() {
            const tagUrl = `${apiBase}/tags/${encodeURIComponent(tag)}`;
            try {
              const existing = await fetchJson(tagUrl, { headers });
              if (!existing || existing.response.status === 404) {
                const createRes = await fetch(`${apiBase}`, {
                  method: 'POST',
                  headers: {
                    ...headers,
                    'Content-Type': 'application/json',
                  },
                  body: JSON.stringify({
                    tag_name: tag,
                    name: tag,
                    body,
                    draft: false,
                    prerelease: false,
                  }),
                });

                if (!createRes.ok) {
                  const details = await createRes.text();
                  console.error('Failed to create release:', createRes.status, details);
                  process.exit(1);
                }
                console.log(`Created release ${tag} with updated notes.`);
                return;
              }

              if (!existing.response.ok) {
                console.error('Failed to load release:', existing.response.status, existing.json);
                process.exit(1);
              }

              const releaseId = existing.json.id;
              const patchRes = await fetch(`${apiBase}/${releaseId}`, {
                method: 'PATCH',
                headers: {
                  ...headers,
                  'Content-Type': 'application/json',
                },
                body: JSON.stringify({ body }),
              });

              if (!patchRes.ok) {
                const details = await patchRes.text();
                console.error('Failed to update release notes:', patchRes.status, details);
                process.exit(1);
              }

              console.log(`Updated release ${tag} notes successfully.`);
            } catch (error) {
              console.error('Error while updating release notes:', error);
              process.exit(1);
            }
          }

          main().catch((error) => {
            console.error('Unexpected error while updating release notes:', error);
            process.exit(1);
          });
          NODE

      - name: Use npm with trusted publishing support
        if: ${{ steps.state.outputs.publish == 'true' }}
        run: |
          npm install -g npm@^11.18.0
          npm --version

      - name: Verify trusted-publishing environment
        if: ${{ steps.state.outputs.publish == 'true' }}
        run: |
          node --version
          npm --version
          npm config get registry
          npm config delete //registry.npmjs.org/:_authToken || true
          test -n "${ACTIONS_ID_TOKEN_REQUEST_URL:-}" || { echo "ACTIONS_ID_TOKEN_REQUEST_URL missing; OIDC not available"; exit 1; }
          test -n "${ACTIONS_ID_TOKEN_REQUEST_TOKEN:-}" || { echo "ACTIONS_ID_TOKEN_REQUEST_TOKEN missing; OIDC not available"; exit 1; }

      - name: Publish to npm
        if: ${{ steps.state.outputs.publish == 'true' }}
        run: |
          npm publish --access public

      - name: Wait for npm package visibility
        if: ${{ steps.state.outputs.publish == 'true' }}
        env:
          PACKAGE_VERSION: ${{ steps.state.outputs.version }}
        run: |
          for attempt in $(seq 1 30); do
            if PUBLISHED_VERSION=$(npm view "c64bridge@${PACKAGE_VERSION}" version --json 2>/dev/null | tr -d '"'); then
              :
            else
              PUBLISHED_VERSION=""
            fi
            if [ "$PUBLISHED_VERSION" = "$PACKAGE_VERSION" ]; then
              echo "c64bridge@${PACKAGE_VERSION} is visible on npm"
              exit 0
            fi
            echo "Waiting for c64bridge@${PACKAGE_VERSION} to appear on npm (attempt ${attempt}/30)"
            sleep 10
          done

          echo "Timed out waiting for c64bridge@${PACKAGE_VERSION} to appear on npm" >&2
          exit 1

      - name: Validate MCP Registry manifest
        run: ./mcp-publisher validate server.json

      - name: Authenticate to MCP Registry
        if: ${{ steps.state.outputs.publish == 'true' }}
        run: ./mcp-publisher login github-oidc

      - name: Publish to MCP Registry
        if: ${{ steps.state.outputs.publish == 'true' }}
        run: ./mcp-publisher publish server.json

      - name: Logout from MCP Registry
        if: ${{ always() }}
        run: ./mcp-publisher logout || true

  verify_npm:
    needs:
      - release
    uses: ./.github/workflows/package-check.yaml
    with:
      package_source: npm
      package_version: ${{ needs.release.outputs.pkg_version }}
